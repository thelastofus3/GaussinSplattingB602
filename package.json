{
  "name": "b602-gaussian",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"node server/server.js\"",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add dist && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^6.0.3",
    "vite-plugin-cross-origin-isolation": "^0.1.6"
  },
  "dependencies": {
    "@mkkellogg/gaussian-splats-3d": "^0.4.6",
    "concurrently": "^9.1.2",
    "express": "^4.21.2"
  }
}
